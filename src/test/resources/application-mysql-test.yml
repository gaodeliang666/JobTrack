spring:
  datasource:
    url: ${JOBTRACK_TEST_DB_URL}
    username: ${JOBTRACK_TEST_DB_USERNAME}
    password: ${JOBTRACK_TEST_DB_PASSWORD}
  flyway:
    clean-disabled: true
    locations: classpath:db/migration

jobtrack:
  current-user-id: ${JOBTRACK_TEST_CURRENT_USER_ID}
